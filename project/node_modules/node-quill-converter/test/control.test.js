const { iterate } = require('leakage');
 
function add(a, b) {
  return a + b;
}

describe('myLib', () => {
  it('does not leak when doing stuff', () => {
    iterate(() => {
        add(5, 3);
    })
  })
})